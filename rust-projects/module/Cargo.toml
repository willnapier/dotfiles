[package]
name = "module"
version = "0.1.0"
edition = "2021"
description = "Module management for William's portable context scrolls"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
walkdir = "2"
