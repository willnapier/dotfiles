[package]
name = "clinical-dashboard-iced"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.13", features = ["tokio"] }
tokio = { version = "1", features = ["process", "io-util", "rt-multi-thread", "macros"] }
chrono = "0.4"
