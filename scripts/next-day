#!/usr/bin/env nu
# Self-contained next-day for Helix keybinding (Space+])
# Writes resolved path to /tmp/helix-day-target.txt
# MUST be self-contained — config.nu is NOT loaded in script mode

let target_date = ((date now) + 1day | format date "%Y-%m-%d")
let note_path = $"($env.HOME)/Forge/NapierianLogs/DayPages/($target_date).md"
$note_path | save -f /tmp/helix-day-target.txt
