#!/bin/bash
# Helix integration for concert-capture (Space+D)
# Finds latest venue HTML in Downloads or Captures/web-archives,
# archives it to Captures/concerts/, prints the concert.{venue}:: line
# for Helix :insert-output.

~/.local/bin/concert-capture --latest --entry-only
