#!/usr/bin/env nu

# Assistants Documentation Real-Time Sync - Re-nu Native Version
# Watches ~/Assistants directory and auto-commits + pushes on file changes
# Re-nu conversion: No fswatch dependency, pure Nushell structured data processing

def main [] {
    let assistants_dir = $"($env.HOME)/Assistants"
    let log_file = "/tmp/assistants-docs-watcher.log"
    let lockfile = "/tmp/assistants-docs-watcher.lock"
    
    # Ensure log file exists
    touch $log_file
    
    # Check for existing instance
    if ($lockfile | path exists) {
        let existing_content = (try { open $lockfile | str trim } catch { "" })
        if $existing_content != "" {
            log_message $log_file $"Assistants docs watcher already running: ($existing_content)"
            return
        }
        log_message $log_file "Removing stale lock file"
        rm $lockfile
    }

    # Create lock file with timestamp
    (date now | format date "%s") | save $lockfile

    # Validation checks
    if not ($assistants_dir | path exists) {
        log_message $log_file $"ERROR: Assistants directory not found at ($assistants_dir)"
        cleanup $lockfile $log_file
        return
    }

    let git_dir = $"($assistants_dir)/.git"
    if not ($git_dir | path exists) {
        log_message $log_file $"ERROR: ($assistants_dir) is not a git repository"
        cleanup $lockfile $log_file
        return
    }

    log_message $log_file $"🚀 Starting Assistants docs watcher for ($assistants_dir)"
    log_message $log_file "📝 Monitoring .md files for auto-commit and push"
    log_message $log_file "⚡ Using native Nushell watch - no fswatch dependency"

    # Native Nushell watch with structured data
    try {
        watch $assistants_dir --glob "**/*.md" --debounce-ms 1000 {|operation, path, new_path|
            handle_docs_change $operation $path $new_path $assistants_dir $log_file
        }
    } catch {|error|
        log_message $log_file $"❌ Watch failed: ($error.msg)"
        cleanup $lockfile $log_file
    }
}

# Logging function
def log_message [log_file: string, message: string] {
    let timestamp = (date now | format date "%Y-%m-%d %H:%M:%S")
    let log_entry = $"($timestamp) - ($message)"
    print $log_entry
    $log_entry | save --append $log_file
}

# Cleanup function
def cleanup [lockfile: string, log_file: string] {
    if ($lockfile | path exists) {
        rm $lockfile
    }
    log_message $log_file "Assistants docs watcher stopped"
}

# Handle documentation file changes with native structured data
def handle_docs_change [operation: string, file_path: string, new_path: any, assistants_dir: string, log_file: string] {
    # Only process actual file modifications to markdown files
    if ($operation == "Modify") and (($file_path | path parse).extension == "md") {
        log_message $log_file $"📝 Detected change in: ($file_path)"

        # Small delay to ensure file write is complete
        sleep 500ms

        # Change to Assistants directory for git operations
        cd $assistants_dir
        
        # Check if there are actually any changes using git
        let git_status = (do { git status --porcelain } | complete)
        if $git_status.exit_code != 0 {
            log_message $log_file "❌ Failed to check git status"
            return
        }
        
        if ($git_status.stdout | str trim | is-empty) {
            log_message $log_file "ℹ️  No actual changes detected after checking - skipping"
            return
        }
        
        # Add all changes
        let git_add = (do { git add . } | complete)
        if $git_add.exit_code != 0 {
            log_message $log_file $"❌ Failed to add changes: ($git_add.stderr)"
            return
        }
        
        # Create commit with file-specific message
        let filename = ($file_path | path basename)
        let timestamp = (date now | format date "%Y-%m-%d %H:%M:%S")
        let commit_msg = $"Auto-sync: Updated ($filename)

($timestamp)

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"
        
        # Commit changes
        let git_commit = (do { git commit -m $commit_msg } | complete)
        if $git_commit.exit_code == 0 {
            log_message $log_file $"✅ Committed changes for ($filename)"
            
            # Push to remote
            let git_push = (do { git push origin main } | complete)
            if $git_push.exit_code == 0 {
                log_message $log_file $"🚀 Successfully pushed ($filename) to GitHub"
            } else {
                log_message $log_file $"⚠️  WARNING: Failed to push to GitHub - ($git_push.stderr)"
            }
        } else {
            if ($git_commit.stderr | str contains "nothing to commit") {
                log_message $log_file $"ℹ️  No changes to commit for ($filename)"
            } else {
                log_message $log_file $"❌ Failed to commit: ($git_commit.stderr)"
            }
        }
    } else {
        # Log other events but don't process them
        let filename = ($file_path | path basename)
        log_message $log_file $"👁️  Detected ($operation) on ($filename) (skipped)"
    }
}