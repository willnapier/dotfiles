# ⚠️  CLAUDE CODE: THIS FILE IS DOTTER-MANAGED - EDIT HERE IN DOTFILES, NOT ~/.config/shell/
# ⚠️  NEVER EDIT ~/.config/shell/zshrc - IT'S A SYMLINK TO THIS FILE
# Clean minimal zsh config - NO RECURSION
export PATH="$HOME/.local/bin:$PATH"
export PATH="$HOME/.cargo/bin:$PATH"
export EDITOR="hx"
export VISUAL="hx"

# Forge path for Zellij link picker
export FORGE="/Forge"

# API Keys - Set these in ~/.zshenv or another secure location
# DO NOT commit API keys to git!
# export ANTHROPIC_API_KEY="sk-ant-..."
# export OPENROUTER_API_KEY="sk-or-..."
# Switch to Nushell for interactive use
if [[ $- == *i* ]] && command -v nu &> /dev/null; then
    exec nu
fi
export PATH="$HOME/.local/bin:$PATH"

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/williamnapier/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/williamnapier/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/williamnapier/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/williamnapier/google-cloud-sdk/completion.zsh.inc'; fi
