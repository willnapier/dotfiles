[Unit]
Description=Refresh Continuum assistant allocation usage and notify opportunities
After=graphical-session.target

[Service]
Type=oneshot
Environment=HOME=/home/will
Environment=PATH=/home/will/.local/bin:/usr/local/bin:/usr/bin
ExecStart=/home/will/.local/bin/continuum usage codex --refresh --notify --quiet --provenance scheduled

