[Unit]
Description=Zotero Import Bridge - Native Nushell Re-nu Version
After=network.target

[Service]
Type=simple
ExecStart=%h/.local/bin/zotero-bridge-renu
Restart=on-failure
RestartSec=30
StandardOutput=journal
StandardError=journal

Environment="PATH=%h/.local/bin:%h/.cargo/bin:/usr/local/bin:/usr/bin:/bin"
Environment="HOME=%h"

[Install]
WantedBy=default.target