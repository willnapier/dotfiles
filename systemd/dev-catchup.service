[Unit]
Description=Generate the weekly DevLog
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=%h/.local/bin/dev-catchup --devlog --write
Environment=PATH=%h/.local/bin:%h/.cargo/bin:/usr/local/bin:/usr/bin:/bin
Environment=HOME=%h

[Install]
WantedBy=default.target
